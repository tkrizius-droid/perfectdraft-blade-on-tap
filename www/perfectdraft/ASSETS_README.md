# PerfectDraft + Blade artwork assets

This folder is installed to Home Assistant as:

`/config/www/perfectdraft/`

and is referenced by the dashboard as:

`/local/perfectdraft/`

Expected subfolders:

- `blade/`
- `glass/`
- `flags/`

PerfectDraft keg PNG files live directly in this folder.
