/* PerfectDraft + Blade On Tap
 * HACS dashboard entry point.
 * The visual dashboard itself is YAML-based; this file exists so HACS can
 * manage the repository's dashboard assets. No custom element is registered.
 */
console.info("[PerfectDraft + Blade On Tap] HACS assets loaded");
